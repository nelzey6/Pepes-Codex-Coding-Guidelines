Start a long-running coding task.

Follow `AGENTS.md`, read `PROJECT.md`, and use `PLANS.md`. Do not patch
immediately unless I explicitly say `implement directly`.

Create or continue:

```text
docs/runs/<date>-<slug>/STATUS.md
```

Goal:
<Describe the coding goal here.>

Before coding, record evidence, architecture/root-cause stance, complexity
budget, validation plan, and next action. Then implement, validate, reassess,
and update `STATUS.md` until done or blocked by a user-owned decision.
